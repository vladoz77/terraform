[all:vars]
ansible_python_interpreter=/usr/bin/python3

[master]
%{ for name, ip in masters ~}
k8s-${name} ansible_host=${ip}
%{ endfor ~}

[worker]
%{ for name, ip in workers ~}
k8s-${name} ansible_host=${ip}
%{ endfor ~}